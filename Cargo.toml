[workspace]
resolver = "2"
members = [
    "agents",
    "models",
    "utils",
    "app",
]

[workspace.dependencies]
reqwest = { version = "0.11", features = ["blocking", "json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"